package ai.djl.huggingface.tokenizers;

/** Minimal HuggingFaceTokenizer stub for compile-time. */
public class HuggingFaceTokenizer {

    public static HuggingFaceTokenizer newInstance(String model) {
        return new HuggingFaceTokenizer();
    }

    public Encoding encode(String input) {
        return new Encoding();
    }
}
