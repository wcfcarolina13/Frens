package ai.djl.ndarray;

/** Minimal NDArray stub. */
public class NDArray {
    public NDArray expandDims(int axis) { return this; }

    public NDArray reshape(int a, int b) { return this; }

    public NDArray softmax(int axis) { return this; }
}
