package ai.djl.ndarray;

/** Minimal NDList stub. */
public class NDList {

    public NDList(NDArray... arrays) {}

    public NDArray get(int i) { return new NDArray(); }

    public NDArray singletonOrThrow() { return new NDArray(); }
}
